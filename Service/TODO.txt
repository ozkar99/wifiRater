Crear un servicio RESTfull con algun framework chevere.

Igualmente crear una base da datos MySQL o lo que sea como mirror a la de la aplicacion.
La aplicacion usara su SQLite como cache.

DB:
Create table Ratings (id primary key integer, rating Integer, bssid char(50));
Create table Users (user varchar(30), passwd varchar(30));

Mas o menos...

QUe nos regrese un JSON con los ratings al darlse un GET:

http://se2.ozkar.org/Ratings/1

Ejemplo:
{
bssid: 00:11:22:33:44:55,
rating: 5
}

O bien sin id que nos regrese un select *.

Luego esto se parsea y se mete al SQLite de la aplicacion y de ahi la aplicacion la carga.

Para darle rate se actualiza la version MySQL en internet.
